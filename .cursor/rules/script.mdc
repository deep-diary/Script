---
description: MATLAB 函数注释与定义规范（适用于所有函数文件）
globs: *.m
alwaysApply: true
---
适用于项目内所有 MATLAB 函数文件（`.m`）。输出风格需对齐 MATLAB 官方 `help` 文档格式，并满足以下规则：

1) 函数头与注释结构（强制）
- 主函数定义后必须紧跟 H1 行：`%FUNCNAME 一句话摘要`（函数名大写）。
- 注释块按顺序包含：
  - 语法（Syntax）
  - 功能描述（Description）
  - 输入参数（Input Arguments）
  - 可选参数（Name-Value）
  - 输出参数（Output Arguments）
  - 异常与边界行为（Errors/Warnings）
  - 使用示例（Examples，至少 2 个）
  - 参见（See also）
  - 元信息（作者、版本、日期、变更记录）

2) 参数与默认值（强制）
- 所有公共函数必须进行参数解析与校验（推荐 `inputParser` + `validateattributes` / `mustBe*`）。
- Name-Value 参数必须写清默认值、可选值、约束。
- 注释中的默认值必须与代码实现一致。

3) 示例与可维护性（强制）
- 至少提供：
  - 一个最简调用示例
  - 一个带 Name-Value 的典型示例
- 示例应与项目上下文一致，可直接理解复用。
- 复杂逻辑拆分为本地子函数（建议命名 `i_xxx`）。

4) 错误处理与行为声明（强制）
- 预期失败路径明确：何时 `error`，何时 `warning`。
- 报错信息包含函数名与关键上下文（文件/参数/模型名等）。
- 若函数有副作用（写文件、修改模型、覆盖内容），必须在注释中显式声明。

5) 变更记录（强制）
- 每次功能性修改更新“变更记录”：`日期 + 版本 + 变更摘要`。
- 推荐版本策略：
  - `1.0` 首次创建
  - `1.1` 兼容增强
  - `2.0` 不兼容变更
- 元信息中的默认作者信息统一使用：
  - `作者: blue.ge(葛维冬@Smart)`
  - 如用户或文件内已有明确作者要求，以用户指定为准。

6) 最小必填项检查（提交前）
- H1 行
- 语法
- 输入参数
- 可选参数（如有）
- 输出参数
- 示例
- 作者/版本/日期/变更记录

若用户要求“优化脚本/重构函数/补全文档”，默认按本规则执行。