---
description: 开源化目录组织与命名约束（适用于项目级开发）
globs: "**/*"
alwaysApply: true
---
适用于整个项目的目录与命名治理，目标是逐步演进为专业开源工程。开发过程中请遵守以下约束：

1) 顶层目录职责约束（新增代码默认落位）
- 源码放 `src/`
- 示例放 `examples/`
- 测试放 `tests/`
- 文档放 `docs/`
- 工具脚本放 `tools/`
- 资源放 `resources/`
- 样例数据放 `data/`
- 三方依赖放 `third_party/`

2) 源码与产物分离（强制）
- 不要把生成产物直接放进源码目录。
- 模型缓存、导出表、临时结果统一放入：
  - `cache/`
  - `artifacts/`
  - `artifacts/tmp/`
- 新增会持续生成的文件类型时，同步补充 `.gitignore` 规则。

3) 命名规范（强制）
- 目录命名使用 `lower_snake_case`，避免空格。
- MATLAB 函数文件名必须与函数名一致。
- 禁止仅通过大小写区分文件/目录名称（避免跨平台冲突）。
- 自动生成文件建议“稳定前缀 + 时间戳”命名。

4) MATLAB 组织约束（强制）
- 可复用逻辑必须封装为函数并放源码目录，不将业务逻辑写死在入口脚本。
- 脚本入口与实现分离：入口放 `examples/` 或 `tools/`。
- 模块按能力分层，命名保持一致（如 `find*`、`create*`、`change*`、`delete*`）。

5) 渐进迁移策略（执行约束）
- 允许保留历史目录，但新增内容必须优先使用目标结构。
- 涉及目录重构时，采用“兼容包装 + 分批迁移”方式，避免一次性大改。
- 若改动影响外部调用路径，必须提供迁移说明。

6) 开源元信息约束（建议优先）
- 逐步补齐并维护：`LICENSE`、`CONTRIBUTING.md`、`CODE_OF_CONDUCT.md`、`CHANGELOG.md`。
- README 需包含安装、快速开始、目录说明与示例入口。

当任务涉及“新增模块、重构目录、生成文件、命名调整、开源化整理”时，默认按本规则执行。
